{
  "name": "silly-merchants-beta",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "sillyworld-client"
  ],
  "scripts": {
    "build": "cd sillyworld-client && npm install --production=false && npm run build",
    "start": "cd sillyworld-client && npm start",
    "heroku-postbuild": "cd sillyworld-client && npm install --production=false && npm run build"
  },
  "engines": {
    "node": "18.17.0",
    "npm": "10.2.0",
    "yarn": "0.0.0"
  }
} 